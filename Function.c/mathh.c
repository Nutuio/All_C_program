#include<stdio.h>
#include<math.h>
int main()
{
    float a=0.5;
    float w,x,y,z;
    w=sin(a); //invoking 
    x=cos(a); //invoking
    y=tan(a); //invoking
    z=pow(a,2);//invoking
    printf("%f %f %f %f\n",w,x,y,z);
    return 0; 
}